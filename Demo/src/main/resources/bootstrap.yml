spring:
    application:
        name: Demo
    cloud:
        config:
            uri: ${CONFIG_SERVER_URL:http://localhost:8888}
            fail-fast: true
            password: ${CONFIG_SERVICE_PASSWORD}
            username: user
